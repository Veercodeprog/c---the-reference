#include <iostream>
#include <string>

int main(){
/*
std::cout << "Hello c++" << std::endl;

int age{21};

std::cout << "My age is " << age << std::endl;

std::cerr << "Error : something wrong" << std::endl;
std::clog << "Log : something wrong" << std::endl;
  
    
    */

    /*
    int age1;
    std::string name;

    std::cout << "Enter your name and age: "  << std::endl;

    std::cin >> name;
    std::cin >> age1;

    std::cout << "Your name is " << name << " and your age is " << age1 << std::endl;

    */ 

     std::string full_name;
    int age3;

    std::cout << " Please type in your full name and age: " << std::endl;
/* 
        std::getline is used to read an entire line of text, including spaces, from the standard input (usually the keyboard) and store it in the full_name variable.
It reads input until it encounters a newline character (Enter key), which means it can capture spaces and special characters.

    */ 

    std::getline(std::cin , full_name);
    std::cin >> age3;

    std::cout << "Your name is " << full_name 
          << " and your age is " << age3 << std::endl;
    return 0;
}
